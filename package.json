{
  "name": "@mwaeckerlin/scratch",
  "version": "1.1.1",
  "scripts": {
    "build": "docker compose build",
    "test": "npm run test:image",
    "test:image": "npm run build && bash tests/image-contract.sh mwaeckerlin/scratch"
  }
}